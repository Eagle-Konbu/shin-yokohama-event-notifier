# Lambda build artifacts
bootstrap
lambda.zip
lambda-daily
lambda-weekly
*.zip

# Terraform state files
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Terraform sensitive configuration
terraform.tfvars

# Test coverage reports
coverage.out
